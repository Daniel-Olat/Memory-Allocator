#ifndef LEAP_H
#define LEAP_H

#include <stdbool.h> // Allows a function to return a boolean value.

bool leap_year(int year);//Takes a year argument and checks whether it is a leap year or not. Returns true if it is a leap year, and false otherwise.

#endif 